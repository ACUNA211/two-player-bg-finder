# Per-count Recommended % columns

Type: task
Status: needs-triage
Blocked by: 10

## What

When a Must play count is checked, add a column with Best + Rec % at that count. This needs the scraper to store the full Player-count poll, not just the poll at 2. Deferred to keep scope small.
